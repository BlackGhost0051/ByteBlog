# ByteBlog

# Run

```bash
docker-compose up -d --build
```

# Stop
```bash
docker-compose down
```

# page

URL : `http://localhost:8080`


# phpmyadmin

URL : `http://localhost:8081`

```
user : root
pass : root
```
